{{ if .Values.SSLChainedCert }}
apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: nginx-secret
data:
  nginx.crt: {{ .Values.SSLChainedCert | b64enc }}
  nginx.key: {{ .Values.SSLPrivateKey | b64enc }}
{{ end }}
