---
kind: Service
apiVersion: v1
metadata:
  name: {{ include "dh-ms-nginx.name" . }}
spec:
  selector:
    app: {{ include "dh-ms-nginx.name" . }}
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  type: NodePort
